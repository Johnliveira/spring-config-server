spring:
  application:
    name: greeting-service
server:
  port: 8080
greeting-service:
  greeting: "Ola"
  default-value: "Mundo"
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: always
